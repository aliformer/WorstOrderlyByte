class InAppEventProvider {
  private readonly 
  
  constructor({
    campaignId,
    userId, 
    deviceId, 
    deviceModel,
    campaignList, 
    
  })

  mapCampaign
  
  getUserInfo(){
    const
  }
  setEvent(){
    const envent 
  }
}